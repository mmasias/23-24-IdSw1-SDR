@startuml descripción CDU informe

skin rose

  :DASHBOARD_ABIERTO;
  :INFORME;
  
partition "ver informe" {
  start
  switch(TIPO INFORME)
    case(DIA)
     :Seleccionar dia;
    case(SEMANA)
     :Seleccionar semana;
    case(MES)
     :Seleccionar mes;
  endswitch

  :mostrarInforme;
  stop
}
@enduml